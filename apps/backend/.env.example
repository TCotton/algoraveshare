# Example .env for backend local development# Example .env for backend local development

# Copy to .env and edit as needed.# Copy to .env and edit as needed.

# If you run Postgres via docker-compose in this repo the service uses Docker secrets# Matches docker-compose.yml in repo root

# (see docker/secrets/*). For local development you can either:DATABASE_URL='postgres://postgres:postgres@127.0.0.1:5432/testDB'
#  - Create a .env file with DATABASE_URL; or
#  - Export DATABASE_URL in your shell. The default secret values are postgres/postgres/testDB.

# Example fallback DATABASE_URL (use only for local dev):
DATABASE_URL='postgres://postgres:postgres@127.0.0.1:5432/testDB'
