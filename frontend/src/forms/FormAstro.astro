---
import Form from '../forms/Form.tsx';
import type { FormProps } from '../forms/formtypes.ts';
const { forms, label, items } = Astro.props as FormProps;
---

<Form forms={forms} label={label} items={items} />