// Theme entry: import the Catppuccin theme CSS so the build includes it.
// The package name was added to package.json as @webtui/theme-catppuccin.
import '@webtui/theme-catppuccin/dist/catppuccin.css'

// If the theme exposes JS API, initialize here. For now this file only
// imports the CSS so it's bundled.

export {}
