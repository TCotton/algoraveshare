{
  "name": "algoraveshare",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "test": "vitest",
    "test:unit": "vitest",
    "install:all": "frontend && backend",
    "frontend": "cd backend && npm ci && npm test",
    "backend": "cd backend && npm ci && npm test",
    "infrastructure": "cd infrastructure && terraform init -backend=false && terraform validate",
    "prepare": "husky",
    "preinstall": "npx only-allow pnpm"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=22"
  },
  "prepare": "cd .. && husky frontend/.husky",
  "devDependencies": {
    "husky": "^9.1.7",
    "turbo": "^2.5.8"
  }
}
